import styled from "styled-components";

const MainContainer = styled.main`
  margin: ${(props) => props.theme.spacings.spacingExtraLarge} ${(props) => props.theme.spacings.spacingSmall} 0 ${(props) => props.theme.spacings.spacingSmall};
`;

export default MainContainer;
